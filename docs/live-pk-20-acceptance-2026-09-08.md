# تسليم الميزة 20 فقط — 2026-09-08

الحالة: **Partial**. توصيل Flutter الموضح أدناه قابل للفحص آليًا؛ لا توجد شهادة قبول للأجهزة أو مطابقة عقد TEAM المنشور. لا انتقال إلى ميزة أخرى.

## ما تغيّر

- `live_room_battle_opponents_sheet.dart` يفتح `LiveBattleControls` من مدخل PK الفعلي وبنفس مستودع الغرفة وBloc. القائمة تعرض «إدارة منافسة PK» عندما توجد حالة سابقة.
- `live_battle_controls.dart`: اختيار 1v1/TEAM، مدة ضمن 30–1800، طرق النقاط المعروفة بالموديل وهدية من كتالوج GetGiftsUseCase، BO3، lobby بقبطانين، open-teams والانضمام للفريق الصريح، دعوة زميل بـliveId، مغادرة الزميل وإنهاء القبطان لـPK فقط، STUN/TIME/GLOVE والمضاعف ×2/30 ثانية من المسارات الموجودة. الطلب الجاري لا يتكرر، ولا يُطبق رده على غرفة غادرها المستخدم أو مباراة مختلفة. لا إرسال هدايا من هذه الواجهة.
- `live_battle.dart`: دمج field-by-field يحفظ الغائب ويطبق الصفر وnull، ودمج جزئي للقدرات، وأولوية المقعد الصريح على fallback، وتصحيح نقاط الزميل والقبطان المنافس، وعدم إحياء مباراة منتهية بـACTIVE متأخر أو قبول جولة أقدم ذات roundNumber. نوع round_finished وحده لا يثبت نهاية السلسلة. حالة winner الغائبة لا تصبح تعادلًا مصطنعًا.
- `live_team_battle_grid.dart` مركّب في `LiveRoomStage` وViewer `LiveRoomPage`: الفريق الحالي يسار شريط نقاطه، والقبطان فوق زميله، RTL لا يبدّل الانتماء، ومفاتيح ثابتة حسب liveId. يوجد عرض الجولة والانتصارات وحالة القدرات من snapshot. الفيديو الأساسي فقط موصول حاليًا في TEAM؛ بقية الفيديوهات تعرض «غير متاح» والمقاعد الخالية «فارغ». هذا **ليس نجاح وسائط 2v2**.
- أضيفت اختبارات patch والأزرار والمدخل الحقيقي وتخطيط الفرق وبقاء العناصر مركّبة خلال النقاط/خروج وعودة الزميل. أضيف `trafficSource` وقراءة `getLiveById` إلى fake قديم في اختبار guest-auto-join ليتطابق مع واجهة الإنتاج وبوابة الدخول الموجودة؛ لم يتغير expected ولا كود الضيوف.

## الأجزاء التي حُفظت

لم تُعد كتابة LiveRoomBloc أو LiveViewerBloc أو Socket أو مديري LiveKit الأساسيين أو الإيقاف المؤقت أو كاميرا مايا أو مقاعد الضيوف. لم يتغير Backend أو قاعدة بيانات أو إعداد سيرفر أو dependencies/signing. بوابة التذكرة في LiveViewerBloc وCheckoutSnapshot ومسارات الألعاب والإعادة والقصاصات والنادي والمتجر والترويج بقيت كما هي. التعديل السابق في Podfile.lock لم يُعدّل بأوامر هذه الجولة؛ دخل لاحقًا في commit خارجي.

## العوائق المحددة والخطوة المطلوبة

| المجال | المتبقي الفعلي | المطلوب قبل الإغلاق |
|---|---|---|
| عقد TEAM/BO3/قدرات | features-ar/endpoints2/P1/P2 غير موجودة بالشجرة أو app_like_tiktok.zip المفحوص. توصيل المسارات الحالية لا يثبت أظرفها المنشورة، أدوار القدرات، أو معنى نهاية الجولة/السلسلة. | توفير الوثائق الحالية أو استجابات اختبار منقحة مع الأدوار وأنواع أحداث roster/round/finished، وتعريف التعادل والحقول الغائبة/nullable. |
| توكنات الوسائط | لا عقد مشاهدة للأطراف الثلاثة الأخرى. POST join القائم يفتح viewer session ويزيد viewers لغير المضيف وفق mobile-api §6. لا افتراض أن cohosts[] يحتوي live3/live4. | endpoint/response لتوكنات مشاهدة فقط، liveId وroomName وهوية مضيف LiveKit كلٌ بحقل مستقل، TTL والتجديد والإلغاء وصلاحيات الاشتراك، وأثرها على attribution/counters. لا كلمات مرور أو tokens في المحادثة. |
| تنسيق Bloc/Socket مع مايا | المضيف يحوّل LiveHudBattleEvent إلى LiveRoomBattleChanged دون نقل updateType؛ لذلك لا تكفي حماية الموديل من حدث مباراة مختلفة وحدها. المشاهد يعيد emit لـbattle ملتقط قبل انتظار اتصال الوسائط، ما يحتاج حماية من استبدال score/round أحدث. أحداث غادرت roster يمكن أن تعيد تشغيل اتصال إذا لم تُفحص عضوية live الحالية. | تنسيق تعديل محدود للأحداث وحراسة هوية الغرفة/المباراة والجولة ومصدر snapshot، وتحديث الحالة الحالية بعد awaits، دون لمس pause/guest/camera. ثم اختبارات Bloc وSocket فعلية للمسارين. |
| LiveSecondaryRooms | أُصلح المدير القائم بعد اختبارات سباق فاشلة: إبطال sync الأقدم، فصل ملكية slot قبل انتظار disconnect، تنظيف listener/room عند الفشل، تسلسل acquire/release وتنظيف آخر غرفة بعد تحرير الصوت. `videoTrackFor` يقبل هوية LiveKit صريحة ويختار كاميراها فقط، ويُحدّث العرض بأحداث participant/track. لا توجد وصلة TEAM حقيقية تستمد تلك الهوية والتوكنات بعد. | **11 اختبارًا** للمدير ناجحة، و33 مع انحدارات وسائط PK وعقود الفريق الحالية. المطلوب المتبقي: مصدر الهوية والتوكنات الموثق وربط المضيف والمشاهد، ومنع ازدواج opponent/secondary عند دمجهما ثم القبول الفعلي. لا يوجد اختبار صوت native على الأجهزة. |
| قبول الأجهزة | هاتف iPhone واحد ظاهر لاسلكيًا؛ adb فارغ ولا محاكي booted. المثبت iOS com.example.bimobondapp بإصدار 1.0.0 (1) فقط. لا حسابات/بيئة/ميزانية مصرح بها في الجلسة. | أربعة ناشرين مستقلين بوسائط فعلية وحساب مشاهدة، وبيئة وحسابات مصرح بها مع نطاق ظهور البث ورصيد اختبار. لا بث عام أو صرف رصيد قبل هذا التفويض. |

لا يوجد دليل فشل HTTP من Backend الفعلي في هذه الجولة؛ العائق هنا غياب العقد/التنسيق/موارد القبول، وليس اتهامًا بأن الخادم المنشور معطّل. لم تُنشأ توكنات أو broadcasts أو جلسات مشاهدة إضافية للاختبار.

## خطوات القبول التي يعيدها المدير بعد توفير الموارد وإغلاق الربط

كل الحالات التالية **Not run على الأجهزة**. نجاح unit/widget test لا يغيّر هذه الحالة.

1. تحقق من package/version ومصدر البناء على كل جهاز قبل التشغيل. Android الحالي `com.dubai.bimobondapp`، iOS الحالي `com.example.bimobondapp`؛ لا تحذف تطبيقًا قديمًا أو بياناته تلقائيًا.
2. افتح `lib/main.dart` ببناء مميز، وسجّل أدوار A/B/C/D والمشاهد دون أسرار. ابدأ بثوث الاختبار الأربعة وفق التفويض. فيديو لكل حساب مختلف، وليس أربعة ضيوف بغرفة واحدة.
3. A: خيارات البث → بدء منافسة → 1v1 → B → تحدّي. المتوقع POST `/lives/A/battle` مرة واحدة، ثم انتهاء PK يبقي LIVE والكاميرا والصوت قائمين.
4. A: TEAM → مدة معتمدة → scoringMode وscoringGiftId عند الحاجة → BO3 → B. المتوقع start مرة واحدة دون teammate ids، وlive1=A/live2=B ومقعدان فارغان.
5. C: TEAM → الفرق المفتوحة → انضم للفريق 1. المتوقع POST `/lives/C/battle/:battleId/join` مرة واحدة وbody `team:1`. B يدعو D: POST `/lives/B/battle/:battleId/invite` مرة واحدة و`teammateLiveId:D`. roster النهائي الفريق1=A+C والفريق2=B+D.
6. أثبت أربعة فيديوهات بهويات مضيفين صحيحة، وعدم تكرار الصوت، وثبات RTL لكل من A وB وC وD والمشاهد. العدد الحالي في Flutter TEAM **فيديو أساسي واحد**؛ لا يُتجاوز هذا الفشل في التسليم.
7. أرسل حدث نقاط رسمي وفق scoringMode؛ إن احتاج هدية فبرصيد وتفويض اختبار فقط. سجّل طلب الإرسال وحدث score دون أسرار. `live1Score/live2Score` نقاط الفريق من السيرفر دون جمع محلي.
8. C يغادر مرة واحدة عبر `/lives/C/battle/:battleId/leave`، ثم يعود، وأوصل roster متأخرًا وأعد اتصال طرف. لا يزيل disconnect قديم اتصالًا جديدًا، ولا تتوقف كاميرا البث الأساسي، ولا يُنسب الزميل للخصم.
9. نفّذ جولات BO3 والتعادل والقدرات STUN/TIME/GLOVE والمضاعف حسب العقد؛ سجّل roundNumber/wins1/wins2/endTime والنتيجة الرسمية. score متأخر من جولة/مباراة سابقة لا يعيدها. أعد بناء واجهة النقاط وتأكد أن textures/الغرف لم تنفصل.
10. القبطان ينهي PK من زر إنهاء PK؛ المتوقع POST `/lives/:captain/battle/:id/end` مرة واحدة و**صفر** POST `/lives/:id/end`. أثبت استمرار LIVE والكاميرا والصوت لجميع الأطراف. اختبر أيضًا النهاية الرسمية لـLIVE كحالة منفصلة.

أرفق لكل خطوة المتوقع/الفعلي وعدد الطلبات وحالة كل طرف وصورة أو تسجيل منقح من الجهاز. تفاصيل الأوامر والنتائج والبناء محفوظة في نقطة التسليم الأخيرة داخل `live-17-audit-ar.md`.

## نتائج النسخة النهائية

- المجموعة الكاملة: **430 Passed** (`flutter test --no-pub`).
- التحليل العام: **0 errors، 103 warnings، 347 infos**؛ exit 1، ولم تُخفَ التحذيرات. لا findings على الملفات الجديدة أو المعدلة.
- `git diff --check` ناجح، وformat على الملفات المعدلة فقط.
- [APK النهائي](../../artifacts/pk-20260908-32aff995/bimobond-pk-20260908-32aff995-debug.apk): Android Debug، `com.dubai.bimobondapp`، `1.0.0-pk.20260908.32aff995 (2026090802)`؛ تم التحقق من الحزمة وتوقيع v2.
- مصدر البناء: HEAD `81b69590729c1aa3e80d9762cc6e49483a48feae` مع ملفات العمل غير الملتزمة، وبصمة Dart `32aff995cc49717f6803efd146960253536dd993c99714f11203a6029d8c01fb`. لم يتغير المصدر أثناء البناء.
- APK SHA-256: `3f3a72c61abaa666bddf79fdef6c205685f1288db20ec8231ec1f3785731c77a`، حجمه 259469022 بايت. ملفات build-manifest وdart-source-manifest بجانبه.
- **لا تثبيت أو تشغيل لهذا APK على جهاز، ولا قبول PK حي**. لا يمكن تثبيت APK على iPhone الظاهر في الجلسة؛ لا يُعرض البناء بصفته اختبار أربعة فيديوهات.
